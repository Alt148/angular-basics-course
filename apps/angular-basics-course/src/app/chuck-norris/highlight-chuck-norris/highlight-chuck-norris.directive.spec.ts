import { HighlightChuckNorrisDirective } from './highlight-chuck-norris.directive';

describe('HighlightChuckNorrisDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightChuckNorrisDirective();
    expect(directive).toBeTruthy();
  });
});
